extends SkillResource

func execute(cast_global_position: Vector2, caster: Player):
    # var lava_scene = main_scene.instantiate()
    # Instance.map.add_child(lava_scene)
    # lava_scene.global_position = caster.global_position

    # lava_scene.shoot_direction = (cast_global_position - caster.global_position).normalized()

    pass